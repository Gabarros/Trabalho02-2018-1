<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gabriel.trabalho2">

    <application
        android:name="model.BDConfig"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="activity.Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaCandidatos" />
        <activity
            android:name=".ListaEleitores"
            android:label="@string/title_activity_lista_evento"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EleitorDetalhe" />
        <activity android:name=".ItemEleitor"></activity>

    </application>

</manifest>